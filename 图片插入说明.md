# 图片插入说明

每一关都预留了 **3 个图片位**，现在页面上显示的是虚线占位框。插入图片只需要两步，**不用改任何代码逻辑**。

---

## 一、三个图片位在哪

| 槽位名 | 位置 | 建议文件名 |
| --- | --- | --- |
| `hero` | 关卡标题下方、剧情正文之前 | `l1-hero.png` … `l5-hero.png` |
| `story` | 剧情正文之后、任务说明之前 | `l1-story.png` … `l5-story.png` |
| `result` | 研究结果卡片内部、标题上方 | `l1-result.png` … `l6-result.png` |

（`l1` = 第 1 关，`l2` = 第 2 关……现在共 6 关，第 6 关为 `l6`。）

---

## 二、两步插入

### 第 1 步：把图片文件放进 `assets/levels/` 文件夹

```
assets/
└── levels/
    ├── l1-hero.png      ← 放进去
    ├── l1-story.png
    └── l3-result.jpg    ← 扩展名不限，jpg / webp / svg 都行
```

### 第 2 步：在 `js/levels.js` 里填 `src` 和 `caption`

搜索关卡 id（如 `id: 'l1'`），找到它的 `images` 段：

```js
images: {
  hero:   { src: '', caption: '', slot: '关卡主图', file: 'assets/levels/l1-hero.png' },
  story:  { src: '', caption: '', slot: '剧情插图', file: 'assets/levels/l1-story.png' },
  result: { src: '', caption: '', slot: '结果配图', file: 'assets/levels/l1-result.png' }
},
```

把 `src` 改成图片路径、`caption` 改成图注，例如：

```js
images: {
  hero:   { src: 'assets/levels/l1-hero.png', caption: '1970 年代，临床记录里反复出现的模式' },
  story:  { src: '', caption: '' },
  result: { src: 'assets/levels/l1-result.png', caption: '队列研究 12 年随访的发病风险' }
},
```

保存后刷新页面：该槽位的虚线框就会变成真实图片；`src` 仍为空的槽位继续显示占位框。

---

## 三、几个常见问题

**1. 不想让占位框一直露着？**
打开 `js/config.js`，把 `showImagePlaceholders` 改成 `false`。这样所有空槽位都会隐藏，只显示你已插入的图片。发布正式版时建议设为 `false`。

**2. 图片显示"加载失败"？**
最常见是**路径或大小写写错**。Linux 服务器（含 GitHub Pages、VibeDrop）区分大小写，`L1-Hero.PNG` ≠ `l1-hero.png`。确认文件名与 `src` 完全一致。

**3. 图片太大 / 显示不全？**
不用手动裁剪，样式会自动按内容宽度等比缩放（`width: 100%`）。建议图片宽度 1200–1600px、单张控制在 300KB 以内，加载更快。

**4. 想加更多图片位（比如一关要两张剧情图）？**
`app.js` 目前渲染这 4 个具名槽位：`hero`、`story`、`result`、`quiz`。若要用 `quiz`（结论题上方），直接在 `images` 里加一段：

```js
quiz: { src: 'assets/levels/l1-quiz.png', caption: '……', slot: '结论题配图', file: '' },
```

若需要更多自由位置，告诉 AI「在第 N 关的某处再加一个图片位」即可，加一对 `<figure>` 渲染即可。

**5. 想换图片，不换路径？**
直接用同名新文件覆盖 `assets/levels/` 里的旧文件就行，`levels.js` 不用动。

---

## 四、快速自检清单

- [ ] 图片已放入 `assets/levels/`，文件名与 `src` 完全一致（含大小写）
- [ ] `js/levels.js` 中对应关卡的 `src` 已填写，`caption` 已写好
- [ ] 刷新页面，占位框已变成图片
- [ ] 全部图片就位后，把 `js/config.js` 的 `showImagePlaceholders` 设为 `false`
