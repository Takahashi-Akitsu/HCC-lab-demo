# 角色手臂 · 动作方案（草稿 · 待你确认）

> 配套文件：`动作示意图.png`（一眼看全 10 个姿态）、`_pose_tuner.html`（可拖动调参台，双击就能开）
> 结论先放前面：**手从「小臂 + 手」里拆出来单独画，手臂只留 1 张大臂 + 1 张小臂，
> 所有姿态靠肩 / 肘 / 腕三个关节旋转摆出来**。手臂相关要画的图：**14 张 → 8 张**，
> 而且以后加新姿势不用再画图，只加一行角度数字。

---

## 一、为什么要这么改

原来的编号是「六个表情各来一张小臂 + 手」，等于**把姿势焊死在图片里**：

- 6 个表情 × 左右 = 12 张，加上 2 张大臂 = **14 张**；
- 每个姿势都要重新画一遍小臂和手，画错一个角度就得重画；
- 想加个「叉腰」「背后手」的新姿势，还得再画 2 张。

改成「大臂 / 小臂 / 手」三段独立部件后：

- **大臂、小臂各画 1 张**（左右用镜像复用），手的形状单独出图；
- 姿势 = 三个关节的角度，改数字就换姿势；
- 同一个表情可以挂多个变体轮流播放，角色不再一直僵着。

---

## 二、绑定结构：肩 → 肘 → 腕 三级嵌套

```
.puppet-stage
└─ .arm[data-side=L/R]        转轴＝肩 28%/72% @ 42.9%      rotate(α1)   ← 左臂整棵加 scaleX(-1)
   ├─ img  arm-upper.png       （大臂，长 150px）
   └─ .seg                    转轴＝肘 28%/72% @ 53.6%      rotate(α2 − α1)
      ├─ img  arm-fore.png     （小臂，长 200px）
      └─ .hand                转轴＝腕 28%/72% @ 67.9%      rotate(α3 − α2)
         └─ img  hand-<型>.png （手，长 95px）
```

- **手是小臂的子节点**：小臂一转，手自动跟着走，不需要单独算位置。
- 因为角度是「相对父级」的，CSS 里写的是增量（`α2 − α1`、`α3 − α2`），跟旋转轴心天然对齐。
- **左臂 = 右臂整棵子树镜像**（`transform: scaleX(-1)`），所以左右共用同一套图，图量直接砍半。
- 图层 z：左臂默认 `18`（在身体后面），右臂默认 `38`（在身体前面）；
  两者 z 相同时按 DOM 顺序，右臂在后 → 右臂压在上面。

### 锚点（替换现在的 `CHARACTER.ANCHORS` 手臂部分）

| 部件 | 转轴 | x | y | 说明 |
| --- | --- | --- | --- | --- |
| `arm-upper.png` | 肩 | `39.70%` | `56.43%` | 大臂 145px（实测 397,790） |
| `arm-fore.png` | 肘 | `39.80%` | `66.79%` | 小臂 70px（实测 398,935） |
| `hand-*.png` | 腕 | `40.00%` | `71.79%` | 手 68px（实测 400,1005） |

**中性姿势 = 大臂、小臂、手全部垂直向下、三段共线。** 这样三个轴心就是同一条竖线上的三个点
（`y = 600 / 750 / 950 px`），画的时候只要把手臂画成笔直垂下即可，图片放进去自动对齐，不需要任何坐标参数。

---

## 三、画图约定（4 条硬要求）

1. **同画布**：所有部件仍是 **1000 × 1400 px 透明 PNG，导出不裁剪、不移动**（沿用现在的规矩）。
2. **球关节**：肘、腕两个接口都画成**半径相同的圆头**——小臂上下两端是圆头，手在与小臂相接的那一端
   也是同半径圆头，两者**同心重叠约 8–10px**。这样手腕转 ±90° 也看不出接缝；袖口正好压在这个圆头上最自然。
3. **中性打光**：大臂 / 小臂 / 手都别只在右侧打重阴影，否则左臂镜像后明暗会翻，一眼看出是同一张图。
4. **手朝向小臂延长线**：手默认顺着小臂方向画（腕到指尖共线），我把这个叫 `Δ3 = 0`；
   个别姿势再靠 `Δ3` 微调手腕角度，画图上不用管。

---

## 四、10 个姿态（全部由同一套 9 张部件旋转得到）

见 `动作示意图.png` 第 2 节。角度约定 **α：0° = 指向正下方，正值 = 顺时针（朝屏幕右侧 / 身体外侧）**；
Δ2 = 肘弯（小臂相对大臂），Δ3 = 腕（手相对小臂，默认 0）。

### 4.1 你点名的动作对照

| 你的要求 | 落地姿态 | id |
| --- | --- | --- |
| 平时 3 个：垂手 / 手放胸前 / 摆手 | ①垂手 ②右手搭胸口 ③右手抬起轻摆 | `idle-1/2/3` |
| warn-cred 双手半臂交叠在胸前 | 两小臂横在胸前，**右臂压左臂**，左右错开 52px 免得糊成一条 | `warn-cred` |
| warn-cost 一手举到头旁 + 另一手算账 | 右手举到头侧开掌，左手在腹前**搓指（拇指食指相触）** | `warn-cost` |
| warn-both 叉腰 | 双手扶胯、肘外张，手指朝下外张 | `warn-both` |
| pass ①一手脸旁竖大拇指 + 另一手开掌 | 右手竖拇指举到脸侧，左手摊掌 | `pass-1` |
| pass ②双手挥舞 | 双臂举高到脸侧、掌心朝外 | `pass-2` |
| fail ①双手在背后 | 手藏到身后，**正面只见肘**（手臂图层挪到身体后面） | `fail-1` |
| fail ②双手在肩膀前耸肩 | 双臂摊开掌心向上，**肩点整体上移 25px** 做耸肩 | `fail-2` |

### 4.2 完整角度表

> ⚠️ 下面的数字是**旧骨架（肩 42.9%）**的，仅作参考。
> **以 `姿态调参台.html` 为准**：点「导出参数」拿到的 JSON 就是当前真实生效的数值（新实测锚点
> 肩 397,790 · 肘 398,935 · 腕 400,1005），已写进 `js/character.js` 的 `ARM_POSES`。

| 姿态 | 侧 | 大臂 α1 | 小臂 α2 | 肘弯 Δ2 | 手 Δ3 | 腕位置(px) | 手型 | z |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ① 垂手 | L | -4° | -1° | 3° | 0° | 266,950 | relaxed | 19 |
| ① 垂手 | R | 4° | 1° | -3° | 0° | 734,950 | relaxed | 39 |
| ② 手搭胸前 | L | -4° | -1° | 3° | 0° | 266,950 | relaxed | 19 |
| ② 手搭胸前 | R | 30.8° | -69.1° | -100° | 0° | 610,800 | relaxed | 39 |
| ③ 摆手 | L | -4° | -1° | 3° | 0° | 266,950 | relaxed | 19 |
| ③ 摆手 | R | 50.7° | 167.3° | 116.6° | 0° | 880,500 | open | 39 |
| 双手半臂交叠 | L | -8° | 104° | 112° | 0° | 453,752 | cross | 39 |
| 双手半臂交叠 | R | 8° | -104° | -112° | 0° | 547,700 | cross | 39 |
| 抬手 + 算账 | L | -35.2° | 79.2° | 114.4° | 0° | 390,760 | count | 19 |
| 抬手 + 算账 | R | 62.8° | 186.7° | 123.9° | 0° | 830,470 | open | 39 |
| 叉腰 | L | -29.2° | 41.8° | 71° | 0° | 340,880 | hip | 19 |
| 叉腰 | R | 29.2° | -41.8° | -71° | 0° | 660,880 | hip | 39 |
| ① 竖大拇指 + 开掌 | L | -32° | 86.3° | 118.4° | 0° | 400,740 | open | 19 |
| ① 竖大拇指 + 开掌 | R | 58.7° | 188.1° | 129.4° | 0° | 820,480 | thumb | 39 |
| ② 双手挥舞 | L | -74.6° | -181.3° | -106.8° | 0° | 140,440 | open | 19 |
| ② 双手挥舞 | R | 74.6° | 181.3° | 106.8° | 0° | 860,440 | open | 39 |
| ① 双手背后 | L | -22° | 45° | 67° | 0° | 365,880 | relaxed | 18 |
| ① 双手背后 | R | 22° | -45° | -67° | 0° | 635,880 | relaxed | 18 |
| ② 耸肩 | L | -36.8° | 88.6° | 125.4° | 0° | 390,700 | open | 39 |
| ② 耸肩 | R | 36.8° | -88.6° | -125.4° | 0° | 610,700 | open | 39 |

> 表里写的是**屏幕绝对角**，代码里存相对角：`rotate(α1)` / `rotate(α2-α1)` / `rotate(α3-α2)`。
> 想调姿势直接开 `_pose_tuner.html` 拖滑块，满意了把数值抄回来。

### 4.3 三条遮挡规则

- **手在背后**（fail-1）：手臂整棵 z 调成 18（身体 z=30 之后）→ 正面只露肘，手自动被身体挡住。
- **双臂都在身前**（warn-cred / fail-2）：左臂 z 提到 39，靠 DOM 顺序保证右臂压在左臂上。
- **叉腰**：手落在胯外侧（x≈340 / 660），不在躯干剪影里，所以左臂虽然在身体后面也不会被吃掉。

---

## 五、手型：只要画 6 张（已按你的决定合并）

| # | 文件名 | 名称 | 用在哪 | 画法要点 |
| --- | --- | --- | --- | --- |
| 1 | `hand-relaxed.png` | 放松手 | 垂手 / 搭手 / 背后 | 四指并拢微屈、拇指贴侧，腕宽≈小臂宽 |
| 2 | `hand-open.png` | 开掌 | 摆手 / 摊掌 / 挥舞 / **耸肩** | 五指张开，正反面都行，最百搭 |
| 3 | `hand-thumb.png` | 竖大拇指 | pass 点赞 | 握拳，拇指沿小臂延长线竖起 |
| 4 | `hand-cross.png` | 半握 | 抱臂交叠 | 四指弯成弧形贴在一起，用来互相压住 |
| 5 | `hand-hip.png` | 叉腰手 | 叉腰 | 比 relaxed 更短更宽，四指朝下外张、掌根贴胯 |
| 6 | `hand-count.png` | 收钱手 | 算账 | 拇指与食指指尖相触成小圈，其余三指微屈 |

> 原来的 `hand-shrug`（小摊掌）已**合并进 `hand-open`**（耸肩用开掌），少画一张。
> 如果以后觉得耸肩的手型不够贴切，再单独补一张 `hand-shrug.png` 即可，代码里改一个词。

> 手都是「右手版」，左臂用镜像复用。镜像后拇指换到另一边，**正好就是左手**，符合解剖。

---

## 六、变体轮换（可选的加分项）

同一表情挂多个变体后，可以每隔 **8–12 秒**随机换一次姿势（比如 idle 在垂手 → 搭手 → 摆手之间切），
切换时给 `.arm` 加 `transition: transform .45s ease` 就会自然过渡，不用多画一张图，角色立刻"活"起来。

---

## 七、对现有文件的影响

**要删（不再需要）**：`arm-left-upper.png`、`arm-right-upper.png`、
`arm-left-fore-{6 表情}.png`、`arm-right-fore-{6 表情}.png` —— 共 14 个旧名字。

**要新增**：`arm-upper.png`、`arm-fore.png`、`hand-{relaxed,open,thumb,cross,hip,count}.png` —— 共 8 个。

**角色图片总数**：32 张（脸 13 + 头发 5 + 身体 1 + 手臂 8 + 气泡 5），已到位 12 张。

**代码改动点**（已完成，均在 `js/character.js` / `styles.css`，视图层没动）：
1. `js/character.js`：手臂改成「`.arm-wrap` (镜像) → `.arm` (绕肩) → `.seg` (绕肘) → `.hand` (绕腕)」的嵌套结构，
   `LAYERS` 里 4 个手臂图层已移除，新增 `ARM_ANCHORS` 与 `ARM_POSES`（10 个姿态）。
2. `styles.css`：新增手臂嵌套样式 + 手臂几何占位（`.ph-arm-upper / .ph-arm-fore / .ph-hand`）。
3. 同一表情有多个变体时，每 9.5 秒随机轮换一次（`VARIANT_MS`）。
4. 旧文件 `arm-left/right*.png` 的后备逻辑已删（新命名与旧命名不兼容）。

---

## 八、落地状态

| 事项 | 状态 |
| --- | --- |
| `hand-shrug` 合并进 `hand-open` | ✅ 已合并（手型 6 张） |
| 小臂保持笔直（Q 版） | ✅ 中线姿势三段共线，角度表无需改 |
| 绑定结构 + 10 个姿态写进代码 | ✅ 已完成，缺图时跑几何占位 |
| 画图清单 | 📄 `角色图片清单.txt`（32 张，含已到位标记与交付批次） |

**重要更新**：锚点已按你实际交的图**重新实测**（肩 397,790 · 肘 398,935 · 腕 400,1005，
大臂 145 / 小臂 70 / 手 68），代码里的 `ARM_POSES` 也按新比例重算了；
调参台换成了**用你真实图片**的 `姿态调参台.html`。

**调参流程**：开 `姿态调参台.html` 拖滑块 → 把右侧 JSON 里的 `a1 / a2 / tilt / hand` 发我（或直接告诉我怎么改），
我更新 `ARM_POSES` 里的数字。**不用把整个 html 发回来**，只要那几个数就行；
当然整份 html 发来我也能读。
