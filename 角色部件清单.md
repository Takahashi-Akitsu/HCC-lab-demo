# 角色部件清单（分层拆分指引）

角色设定：**肝癌病例患者**，站在关卡页左侧的「研究伙伴」，表情随设计状态变化。

本版本把角色拆成**可独立运动**的图层：脸打底与五官分离、前发拆成三条发丝、
**手臂拆成「肩 → 肘 → 腕」三级嵌套（大臂 / 小臂 / 手，左右共用一套图）**、每个情绪再配一张情绪小气泡。

> 所有部件**共用同一张画布**，放进去就自动生效，不用改任何代码里的坐标。
> 手臂的动作原理、10 个姿态的角度表、手型的画法要点，看 **`动作方案.md`**；
> 要画的东西按批次列在 **`角色图片清单.txt`**（共 28 张）。

---

## 一、画布规格（最重要的一条）

| 项目 | 要求 |
| --- | --- |
| 画布尺寸 | **1000 × 1400 px**（所有部件都用这同一张画布） |
| 格式 | **PNG，透明底** |
| 内容 | 每个文件里**只有自己那一块**，其余区域全透明 |
| 坐标 | 所有部件都按同一套坐标画，导出时**不要裁剪、不要移动** |

**为什么必须同画布**：叠放时每个部件自然对齐在同一原点，网页直接上下叠放，**不需要任何坐标参数**。若改成「各自紧贴裁剪」，就得为每个部件单独调位置，反而容易错位。

页面上角色默认显示宽度 220 px、可放大到 180%（≈396 px），2× 高清屏约需 792 px，所以 **1000 px 宽足够清晰**；高度按 10:14，1000 宽对应 1400 高。

> 画的时候先画一张 1000×1400 的**完整人物**，再按下面的分层切开分别导出——比直接分块画更容易保证对齐。

---

## 二、图层顺序（从后到前）

| z | 图层 | 对应文件 | 说明 |
| --- | --- | --- | --- |
| 10 | `hair-back` | `hair-back.png` | 后发（**最底层**，以后会轻摆） |
| 30 | `body` | `body.png` | 躯干 + 双腿 + 衣服 |
| 36 | `arm-L` 容器 | `arm-upper.png` → `arm-fore.png` → `hand-<手型>.png` | 左臂（身体前，压在右臂之下） |
| 38 | `arm-R` 容器 | 同上（左右共用一套图；右臂整棵镜像） | 右臂（身体前） |
| 50 | `face-base` | `face-base.png` | 脸打底（肤色 + 轮廓，**不含五官**） |
| 51 | `face-feat` | `face-<表情>-open.png` / `-blink.png` | 五官（**叠在脸打底之上**） |
| 55 | `hair-mid` | `hair-mid.png` | 基底头发（**不动**；压在脸上面、前发下面） |
| 60 | `hair-front-1` | `hair-front-1.png` | 前发·左条 |
| 61 | `hair-front-2` | `hair-front-2.png` | 前发·中条 |
| 62 | `hair-front-3` | `hair-front-3.png` | 前发·右条 |
| 70 | `bubble-face` | `bubble-<表情>.png` | 情绪小气泡（短暂出现） |

> **图层顺序（从后到前）：后发 → 身体 → 脸 → 中发 → 前发 → 气泡。**

> 手臂是一棵「肩 → 肘 → 腕」三级嵌套：**大臂绕肩转、小臂绕肘转、手绕腕转**（角度是相对父级的）。
> 左臂在身后、右臂在身前，动起来有纵深。大臂 / 小臂各只有 1 张图，左右共用
> （左臂整棵 `scaleX(-1)` 镜像），**姿势靠角度、不靠新图** —— 详见 `动作方案.md`。

---

## 三、文件清单（共 28 张）

### 1) 脸（11 张）

| 文件名 | 内容 |
| --- | --- |
| `face-base.png` | **脸打底**：脸部肤色、轮廓、（可选）耳朵与脖子衔接。**不要画五官** |
| `face-idle-open.png` | 平静 · 五官（睁眼） |
| `face-idle-blink.png` | 平静 · 眨眼差分（眼睛闭合那一帧，其余不变） |
| `face-warn-cred-open.png` | 担心（证据不足）· 睁眼 |
| `face-warn-cost-open.png` | 肉疼（成本过高）· 睁眼 |
| `face-warn-both-open.png` | 犯难（两样都差）· 睁眼 |
| `face-pass-open.png` | 满意（设计达标）· 睁眼 |
| `face-fail-open.png` | 失落（提交未达标）· 睁眼 |
| `face-warn-blink.png` | ★ **三个为难表情共用**的眨眼差分 |
| `face-pass-blink.png` | 满意 · 眨眼 |
| `face-fail-blink.png` | 失落 · 眨眼 |

**画五官时的要点**
- 只画**眉毛、眼睛、嘴**（可含腮红/汗滴等表情符号），**不要画脸型、肤色**——那是 `face-base`。
- 所有 `-open` **位置必须完全一致**，只改形状；否则切换表情时会"跳脸"。
- 眨眼差分**只把眼睛改成闭眼**，眉毛、嘴、其它元素保持与 `-open` 完全相同。
- **三个为难表情共用 `face-warn-blink.png`**（眼型一样，闭眼帧是同一张）；代码回退顺序
  `face-<表情>-blink.png` → `face-warn-blink.png`（仅 warn-cred/warn-cost/warn-both）。
  idle / pass / fail 眼型不同，不吃共用图。详见 `手型与眨眼拆分参考.txt`。

### 2) 前发（3 张，从左到右）

| 文件名 | 内容 |
| --- | --- |
| `hair-front-1.png` | 前发**左**条 |
| `hair-front-2.png` | 前发**中**条（通常最长、盖在额前） |
| `hair-front-3.png` | 前发**右**条 |

三条各自独立成文件，将来可各自以发根为轴心做不同幅度的飘动。

### 3) 后发 / 基底头发（2 张）

| 文件名 | 内容 |
| --- | --- |
| `hair-back.png` | 后发（**最底层**，以后会做轻微摇摆） |
| `hair-mid.png` | 基底头发（**不动**，压在脸上面、前发下面） |

### 4) 身体（1 张）

| 文件名 | 内容 |
| --- | --- |
| `body.png` | 躯干 + 双腿 + 衣服；**头部位置留空**（由脸覆盖）。**肩线画在 y＝42.9%**，左肩 x＝28%、右肩 x＝72%，手臂接在这里才严丝合缝 |

### 5) 手臂（6 张）★

| 文件名 | 内容 |
| --- | --- |
| `arm-upper.png` | **大臂**（中性：从肩垂直向下，长 145px） |
| `arm-fore.png` | **小臂**（中性：从肘垂直向下，长 70px，末端圆头＝腕关节） |
| `hand-relaxed.png` | 放松手 → 垂手 / 搭手 / 背后 / 抱臂 / 叉腰（万能） |
| `hand-open.png` | 开掌 → 摆手 / 摊掌 / 双手挥舞 / 耸肩 |
| `hand-thumb.png` | 竖大拇指 → 点赞 |
| `hand-count.png` | 收钱手 → 搓指算账 |

> ✂ 已砍掉 `hand-cross.png`（半握）与 `hand-hip.png`（叉腰手）：缺图时自动用 `hand-relaxed.png` 顶
> （`js/character.js` 的 `HAND_ALIAS`），以后想补画随时放进来，代码不用改。

**要点**
- **只画右手版**：大臂、小臂、手都只画一份，左臂由程序整棵镜像复用（镜像后拇指换到另一边，正好是左手）。
- **球关节**：肘、腕的接口都画成**同半径圆头、同心重叠约 8–10px**，这样手腕转 ±90° 也看不出接缝；袖口压在这个圆头上最自然。
- **中性姿势＝大臂、小臂、手全部垂直向下、三段共线**（角色是 Q 版，画直的就行）。
- **中性打光**：别只在右侧打重阴影，否则左臂镜像后明暗会翻。
- 六个情绪名固定为：`idle`、`warn-cred`、`warn-cost`、`warn-both`、`pass`、`fail`。

十个姿态（垂手 / 手搭胸前 / 摆手 / 抱臂交叠 / 抬手+算账 / 叉腰 / 竖拇指+开掌 / 双手挥舞 / 双手背后 / 耸肩）
全部由这 8 张图旋转得到，角度表见 `动作方案.md`，效果图见 `动作示意图.png`。

### 6) 情绪小气泡（5 张，**平静没有**）

| 文件名 | 内容 |
| --- | --- |
| `bubble-warn-cred.png` | 担心表情的小气泡（问号、图形等） |
| `bubble-warn-cost.png` | 肉疼表情的小气泡（汗滴、金币符号等） |
| `bubble-warn-both.png` | 两样都差的小气泡 |
| `bubble-pass.png` | 达标的小气泡（星星、对勾等） |
| `bubble-fail.png` | 未达标的小气泡（省略号、乌云等） |

**气泡的硬性要求**
- **位置：画在画布的左上方**，约在 `x 6%–28%`、`y 2%–18%` 的区域（脸部在 `x 29%–71%`、`y 10%–31%`）。
- **绝对不能覆盖到脸部**——这是硬要求。
- 气泡会**短暂出现**（约 1.8 秒后自动淡出），所以画成"一句话说完就消失"的简洁图形即可，不需要长文案。
- 气泡图里**不要写字**（生成式绘图写字几乎必错）；需要文字时用页面自带的文字气泡兜底。

---

## 四、图层旋转轴心

| 图层 | 轴心（x y） | 含义 |
| --- | --- | --- |
| `hair-back` | 50% 30% | 头顶 |
| `hair-mid` | 50% 22% | 颈部（基底头发，不动） |
| `hair-front-1` | 34% 26% | 左发根 |
| `hair-front-2` | 50% 24% | 中发根 |
| `hair-front-3` | 66% 26% | 右发根 |
| `arm-upper` | **39.70% 56.43%** | 肩（实测 397,790 px；右臂整棵镜像） |
| `arm-fore` | **39.80% 66.79%** | 肘（实测 398,935 px；大臂 145px） |
| `hand-*` | **40.00% 71.79%** | 腕（实测 400,1005 px；小臂 70px，手 68px） |
| `body` | 50% 100% | 脚底 |
| `face-base` / `face-feat` | 50% 22% | 颈部 |

> 手臂的左右两侧**共用右侧这一套锚点**：左臂整棵镜像后就自然落到 28% 那条竖线上。
> 你画图时只要保证同画布、且手臂是笔直垂下即可，不需要标轴心。

---

## 五、放置与命名

```
assets/
└── character/
    ├── README.md
    ├── manifest.json           ← 本清单的机读版
    ├── face-base.png
    ├── face-idle-open.png      face-idle-blink.png
    ├── face-warn-cred-open.png face-warn-cred-blink.png
    ├── face-warn-cost-open.png face-warn-cost-blink.png
    ├── face-warn-both-open.png face-warn-both-blink.png
    ├── face-pass-open.png      face-pass-blink.png
    ├── face-fail-open.png      face-fail-blink.png
    ├── hair-front-1.png   hair-front-2.png   hair-front-3.png
    ├── hair-back.png     hair-mid.png
    ├── body.png
    ├── arm-upper.png      arm-fore.png
    ├── hand-relaxed.png   hand-open.png    hand-thumb.png
    ├── hand-cross.png     hand-hip.png     hand-count.png
    └── bubble-warn-cred.png  bubble-warn-cost.png  bubble-warn-both.png
        bubble-pass.png  bubble-fail.png
```

**可以先只放一部分**：缺哪一张，那一层就退回几何占位（手型缺图会退回 `hand-relaxed.png`），页面照样能跑。
已到位 12 张：`body` / `face-base` / `face-idle-open` / `face-idle-blink` / `hair-back` / `hair-mid` /
`hair-front-1/2/3` / `arm-upper` / `arm-fore` / `hand-relaxed`。

**兼容旧文件**：`face-<表情>.png`（整脸）、`hair-front.png` 仍会被当作后备自动使用；
旧的 `arm-left/right*.png` 命名已废弃（手臂改成 8 张新图，见 `角色图片清单.txt`）。

---

## 六、分部件运动

**手臂已实现**（`js/character.js` 的 `ARM_POSES`）：换表情时把角度写到 `.arm / .seg / .hand` 上，
带 `.45s` 过渡；同一表情下有多个变体时会每 9.5 秒随机轮换一次。

**头发摆动、呼吸起伏待实现**：原理是每个部件一个图层，用 `requestAnimationFrame` 每帧算正弦波
`位移 = 幅度 × sin(时间 × 频率 + 相位)`，给每个部件不同幅度 / 频率 / 相位，就会出现"头发比身体慢半拍"的错频感；
旋转时把 `transform-origin` 设在上面第四节的关节处。等图到位后按实际画风调幅度、频率与相位。
