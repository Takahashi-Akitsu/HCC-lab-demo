# 图片规格与 AI 生成提示词（按关卡 · 按位置）

共 6 关 × 3 个图片位 = 18 张。

---

## 一、尺寸与格式

| 槽位 | 位置 | 建议像素 | 比例 | 文件名 |
| --- | --- | --- | --- | --- |
| `hero` | 关卡标题下方（顶部横幅） | **1600 × 900** | 16:9 | `l1-hero.png` … `l6-hero.png` |
| `story` | 剧情正文之后 | **1600 × 1067** | 3:2 | `l1-story.png` … `l6-story.png` |
| `result` | 研究结果卡片内 | **1600 × 900** | 16:9 | `l1-result.png` … `l6-result.png` |

**为什么是 1600px 宽？**
页面正文容器最大宽度是 736 CSS px；在 2× 高清屏上需要约 1472px 才够清晰，所以 **1600px 宽是"够清晰且不浪费"的甜点值**。再大只会增加体积，画质提升肉眼难辨。

**其他硬要求**

- **最低尺寸**：1200 × 675（低于此在宽屏上会发虚）
- **单张体积**：≤ 300 KB（`.jpg` 质量 80 或 `.webp` 更小）
- **格式**：照片感插画用 `.jpg` / `.webp`；线条图表用 `.png`（更锐利、支持透明）
- **比例不必严格**：样式里是 `width:100%; height:auto`，任何比例都不会变形，只是 18 张保持统一比例会更整齐
- **安全边距**：主体距离四边留 5–8%，避免贴边被圆角裁到

---

## 二、统一风格（让 18 张看起来像一套）

把下面这段作为**公共前缀**，每次都放在提示词最前面：

> 温暖复古的科学编辑插画风格；米白纸感背景，限色为深墨绿、浅黄绿与赭褐；细腻颗粒质感，柔和侧光，构图克制、留白充足；画面中不出现任何文字、数字、字母、水印或签名。
> style: warm vintage scientific editorial illustration, muted paper-white, deep green, pale lime and ochre palette, soft grain, restrained composition, no text, no watermark.

> ⚠️ 关键：**别让 AI 在画面里写字**。生成式模型的文字几乎必然错乱，而精确数字本来就该放在图注（`caption`）里——图注是纯文本，永远清晰。所以本套提示词统一要求"无文字"，图表也只画形状不写刻度。

---

## 三、18 条提示词

### 第 1 关 · 发现关联

**`l1-hero.png`（1600×900）**
> 1970 年代的流行病学研究室。一位研究者站在挂满病例卡片的墙前，桌上的泛黄病历与城市地图摊开，老式打字机与放大镜在侧；窗外是工业时代的城市天际线剪影。中景、平视，光线从左侧窗子斜入。

**`l1-story.png`（1600×1067）**
> 微观与宏观并置：画面左半是数支排列整齐的血样试管（无标签文字），右半是一枚抽象的肝细胞剖面，旁边悬浮着若干乙肝病毒颗粒（球形、表面有纹理）。两者之间用极细的虚线连接，暗示"从样本到人群"的推演。

**`l1-result.png`（1600×900）**
> 抽象化的随访示意图：两条平行的水平时间轴向右延伸，上方一列的人群剪影中较少出现深色肝部标记，下方一列明显更多；用两组长短不同的横条对比差异。纯几何与剪影，无任何数字与文字。

### 第 2 关 · 控制混杂

**`l2-hero.png`（1600×900）**
> 研究者在白板前推敲一张因果网络图：多个圆形节点用箭头互相连接，其中一条清晰的横向通路被浅黄绿高亮，另有几个孤立节点被灰色划去。桌上有变量清单卡片与彩色磁贴。

**`l2-story.png`（1600×1067）**
> 更近的抽象因果图视角：中央是一条"左→中→右"三段式通路（中间节点被浅黄绿点亮），左侧汇入两条来自共同上游的箭头；右下角几个与主通路无关的节点被淡灰处理。整体像一张冷静的图示，克制、无文字。

**`l2-result.png`（1600×900）**
> 三根并列的竖直柱状对比，高度依次递减；柱体为深墨绿—浅黄绿—赭褐三色渐变，柱底有一条细基线。纯图形，无刻度、无数字、无坐标轴文字。

### 第 3 关 · 剂量-反应

**`l3-hero.png`（1600×900）**
> 实验室操作台：一排透明的离心管按浓度阶梯排列（颜色由浅到深），一支移液器停在管口上方；背景是虚化的实验室仪器。俯视略倾斜视角，光线柔和。

**`l3-story.png`（1600×1067）**
> 抽象剂量-反应曲线：一条从左下平滑上升到右上的曲线，曲线下方铺着一列高度递增的细柱作为呼应；坐标轴只画线条，不写任何数字与标签。留白充足。

**`l3-result.png`（1600×900）**
> 五级阶梯式递增柱状图，五根柱子由矮到高排列，颜色由浅黄绿过渡到深墨绿，最高一根带一点赭褐高光。纯图形，无数字、无文字、无图例。

### 第 4 关 · 干预验证

**`l4-hero.png`（1600×900）**
> 社区接种点：一位医护人员正在为儿童接种疫苗，家长在旁等待，队伍向画面深处延伸；场景温暖整洁，墙上贴着海报（海报上不出现可辨读的文字，只保留色块与图形）。

**`l4-story.png`（1600×1067）**
> 人群免疫屏障的抽象表达：一片圆形人群剪影，其中大部分个体被一圈柔和的浅黄绿光环笼罩，少数几个保持灰白无光；画面右侧有一个极简的盾形符号。无文字。

**`l4-result.png`（1600×900）**
> 两组人群剪影左右并列：左侧一组个体身上有较多深色标记，右侧一组明显较少；两组的底色分别是淡赭褐与浅黄绿。纯剪影与色块，无数字、无文字。

### 第 5 关 · 机制补充

**`l5-hero.png`（1600×900）**
> 分子生物学实验室：高通量测序仪的一角、凝胶电泳槽、显微镜与一块内容被抽象成光斑与色带的屏幕；一位研究者戴着护目镜在记录。冷调仪器与暖调纸感背景形成柔和对比。

**`l5-story.png`（1600×1067）**
> DNA 双螺旋的局部特写：一段颜色不同的外来病毒序列插入螺旋并与之缠绕，插入位点附近的一段基因区域被浅黄绿点亮，向外发出细小的放射状线条。纯分子视觉，无文字与符号标签。

**`l5-result.png`（1600×900）**
> 三块并排的抽象数据面板（面板之间用细线分隔）：①散点与条带的分布图；②若干高度不一的柱状；③由小圆点组成的密度点阵。全部只画形状与色块，无坐标轴文字、无数字、无图例。

### 第 6 关 · 主动设计动物实验

**`l6-hero.png`（1600×900）**
> 动物实验设施内部：洁净的独立通风笼架（IVC）整齐排列，几只浅色小鼠在其中活动；一位身着防护服、口罩与手套的研究者在记录。安静、有序、明亮，无血腥元素。

**`l6-story.png`（1600×1067）**
> 概念图示：中央是一枚小鼠肝脏的剖面，肝表面散布几个小结节；旁边依次是抽象的"基因编辑"符号——一段被剪开的 DNA 片段与准星靶点，以及一个被点亮的基因位点。纯图形，无任何文字。

**`l6-result.png`（1600×900）**
> 两组小鼠肝脏的对比特写：左侧一颗表面光滑、色泽均匀；右侧一颗表面布满多个小结节、颜色不均；下方用一长一短两条横条呼应差异。写实但不血腥，无文字与数字。

---

## 四、使用建议

**1. 命名与放置**
生成后按 `l1-hero.png`、`l2-story.png` 这类名字放进 `assets/levels/` 文件夹，然后在 `js/levels.js` 找到对应关卡，把 `images` 里那一项的 `src` 填上：

```js
hero: { src: 'assets/levels/l1-hero.png', caption: '1970 年代，临床记录里反复出现的模式' },
```

**2. 图注写数字，图上不写**
所有具体数值（RR、p 值、发生率）都放进 `caption`。既有排版质感，也避免 AI 文字出错。

**3. 想省事**
只做 6 张 `hero` 也完全可用（一关一张主视觉）。story / result 留空，把 `js/config.js` 里的 `showImagePlaceholders` 设为 `false`，空占位框就会隐藏。

**4. 想微调风格**
- 更写实 → 把"插画"换成"纪实摄影感"，去掉"颗粒质感"
- 更抽象 → 加"极简几何、扁平色块、无细节纹理"
- 换色调 → 把"深墨绿、浅黄绿、赭褐"替换成你想要的三个色名

**5. 一次生成多张的写法**
如果工具支持批量，可以在一条提示词里写：
"生成 6 张同一风格的横版插画，主题分别为：①…②…③…，全部 16:9、无文字、色调统一。"
（风格一致性会更好，但单张细节可能略弱。）

---

# 五、把「角色」塞进插图（肝癌病例患者）

## 5.1 两条素材槽位（请你补上）

生成插图时，角色的形象来自下面两处，**优先级见 5.2 的规则句**：

- 【**角色原案** · 待你提供】：把角色的文字设定贴在这里（性别、年龄、发型、瞳色、服装、配饰、体态、气质……）。
  建议至少写清：① 发型与发色 ② 瞳色 ③ 常穿的服装与配色 ④ 一个辨识度高的配饰（如腕带、挂坠、眼镜）。
- 【**角色草稿** · 待你提供】：把三视图 / 表情草稿作为参考图一并传给生图模型。

## 5.2 加进每条提示词的「规则句」（务必带上）

> 画面中的角色必须与【角色草稿】完全一致（发型、发色、瞳色、脸型、服装、配饰）。
> 若未提供角色草稿，则严格依据以下文字原案绘制：【此处粘贴你的角色原案】。
> 无论有无草稿，同一角色在不同图中必须保持可辨识的同一性。

这句放在**公共风格前缀之后、场景描述之前**，AI 就会优先照草稿画、没草稿才照原案画。

## 5.3 哪些图要出现角色（18 张里选 11 张，约五分之三）

| 图 | 角色出现在画面中的身份与位置 |
| --- | --- |
| `l1-hero` | 作为「被记录的病例之一」，站在研究者侧后方，手里捏着一张化验单 |
| `l2-hero` | 站在因果图白板旁，抬手指向图上一个节点，神情若有所思 |
| `l3-hero` | 画面另一侧，卷起袖子等待抽血，或刚抽完按着棉签 |
| `l4-hero` | 接种队伍中的成年受种者（可抱着孩子），排在队伍中段 |
| `l5-hero` | 坐在医生对面，低头看桌上的报告，护腕/腕带可见 |
| `l1-story` | 缩小比例站在画面右下角，作为观察者看向血样与肝细胞 |
| `l3-story` | 站在剂量-反应曲线旁边，微微仰头看曲线的上升趋势 |
| `l5-story` | 站在 DNA 螺旋与整合位点旁边，成为画面里唯一的「人」的尺度参照 |
| `l1-result` | 站在随访时间轴旁，作为被随访人群中的一员，剪影风格、不抢图表 |
| `l3-result` | 站在递增柱状图旁，抬头看向最高那根柱子 |
| `l5-result` | 站在三块数据面板前阅读，侧身、半身入画 |

**不出现角色的 7 张**：`l2-story`（纯抽象因果图）、`l2-result`（柱状图）、`l4-story`（免疫屏障示意）、`l4-result`（两组人群剪影）、`l6-hero` `l6-story` `l6-result`（动物实验场景，人是研究者与小鼠）。

## 5.4 每张图的「追加提示词」

在你已有的场景提示词后面，接着写下面这一句（连同 5.2 的规则句一起用）：

| 图 | 追加句 |
| --- | --- |
| `l1-hero` | 在研究者侧后方加入这位患者：穿着朴素外套，手里捏着一张化验单，神情略带茫然；人物高度约占画面 1/3，位于右侧，不遮挡研究者与病例墙。 |
| `l2-hero` | 在白板右侧加入他：侧身站立，抬手指向因果图上的一个节点，皱眉思索；人物高度约占画面 1/3，位于右侧前景。 |
| `l3-hero` | 在操作台另一侧加入他：卷起袖子等待抽血，视线避开针头；人物半身入画，位于画面右侧，与离心管形成前后景关系。 |
| `l4-hero` | 把队伍中段的一位成年受种者画成他：排队等待，怀里抱着一个孩子，侧脸朝向前方；人物高度约占画面 1/3。 |
| `l5-hero` | 在医生桌对面加入他：坐着低头看报告，手腕上有一圈浅色腕带；半身入画，位于画面右下角。 |
| `l1-story` | 在画面右下角以缩小比例加入他，作为观察者看向血样与肝细胞；尺寸约为画面的 1/4 高，不遮挡主体结构。 |
| `l3-story` | 在曲线左下方加入他：站立仰头，看向曲线的上升方向；人物高度约占画面 1/3，位于左下角留白处。 |
| `l5-story` | 在 DNA 螺旋右侧加入他：站立、抬手轻触螺旋边缘，作为画面中唯一的人形尺度参照；人物高度约占画面 1/2。 |
| `l1-result` | 在时间轴下方以剪影方式加入他，作为被随访人群中的一员，只保留轮廓与配色，绝不抢图表的可读性。 |
| `l3-result` | 在柱状图右下方加入他：站立抬头，看向最高的柱子；人物高度约占画面 1/4，线条简洁、不与柱体重叠。 |
| `l5-result` | 在面板前方加入他：侧身阅读，半身入画，位于画面下缘，不遮挡任何面板内容。 |

## 5.5 风格可以怎么变

角色的**形象**要统一，但**画风/色调**可以随关卡变化，只要保留三处可辨识特征（发型轮廓、瞳色、服装主色）：

- 第 1、2 关（人群与流行病学）→ 偏复古：米白纸感 + 赭褐 + 深墨绿，线条略带手绘颗粒
- 第 3、4 关（实验室与公共卫生）→ 偏清亮：浅黄绿 + 灰蓝点缀，光线更硬
- 第 5、6 关（分子与动物实验）→ 偏深色：深墨绿主导 + 冷白仪器光，对比更强

想统一就用同一份公共风格前缀；想有层次感就按上面分组微调，但**角色本体的配色与轮廓不要跟着变**，否则玩家会认不出是同一个人。

> 另：关卡页左侧那个会动的小人，用的是 `assets/character/` 里的**透明底部件**（见《角色部件清单.md》），和这里插图里的角色**共用同一套形象设定**，所以两边的发型、瞳色、服装要对齐。
